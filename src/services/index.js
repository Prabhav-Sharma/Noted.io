import { login, signup } from "./authAPI";
import { fetchNotes, addToNotes, updateNote, deleteNote } from "./notesAPI";
import {
  fetchArchives,
  addToArchives,
  deleteFromArchives,
  restoreFromArchives,
} from "./archiveAPI";

export {
  login,
  signup,
  fetchNotes,
  addToNotes,
  updateNote,
  deleteNote,
  fetchArchives,
  addToArchives,
  deleteFromArchives,
  restoreFromArchives,
};
